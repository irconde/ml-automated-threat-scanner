import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'edu.ualr.mia',
  appName: 'Advanced Threat Detection',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
