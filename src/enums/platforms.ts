export enum Platforms {
  Electron,
  Web,
  iOS,
  Android,
}

export enum WorkingMode {
  RemoteServer,
  LocalDirectory,
  IndividualFile,
}

export enum FileFormat {
  OpenRaster = 'ora',
  ZipArchive = 'zip',
}
