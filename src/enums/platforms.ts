export enum Platforms {
  Electron,
  Web,
  iOS,
  Android,
}

export enum WorkingMode {
  MinIO,
  LocalDirectory,
  IndividualFile,
}

export enum FileFormat {
  OpenRaster = 'ora',
  ZipArchive = 'zip',
}
